import R from 'ramda';

const getFurnitureByID = (state, id) => R.prop(id, state.furniture)

export const getFurniture = state => {
 const furniture = R.map(id => getFurnitureByID(state, id), state.furniturePage.ids);
 return furniture;
}